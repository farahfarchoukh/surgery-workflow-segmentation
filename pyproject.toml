[project]
name = "proximie-mle-challenge"
version = "0.1.0"
description = "OR workflow phase segmentation prototype - Proximie Senior ML Engineer take-home"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
