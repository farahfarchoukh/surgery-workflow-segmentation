# CPU-only build: this dev environment has no GPU, and the prototype is designed to be
# CPU-trainable in seconds on synthetic data (see report Sec 4.2.2 for the FPS/scale rationale).
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.4

numpy>=1.26
pyyaml>=6.0
pytest>=8.0
ruff>=0.5  # lint, run in CI - see pyproject.toml [tool.ruff]

# Pure-Python report tooling: no pandoc/node/graphviz available in this environment,
# so PDF generation and diagramming both use pip-only, no-system-binary libraries.
markdown>=3.6
xhtml2pdf>=0.2.16
matplotlib>=3.9

# Local serving demo (src/serve.py) - mirrors the SageMaker Real-Time
# endpoint pattern from the report, runnable locally without a cloud account.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27  # test-only: FastAPI's in-process TestClient needs it
prometheus-client>=0.20  # /metrics endpoint - see report Sec 4.4 Production Monitoring

# Transitive-dependency floors, not used directly: xhtml2pdf/svglib/pyhanko pull in
# old default versions of these with known CVEs (found via `pip-audit`, see CI's
# security job). Pinned here so the resolver can't silently pick the vulnerable ones.
idna>=3.7
pillow>=12.3.0
requests>=2.32.4
setuptools>=78.1.1
urllib3>=2.5.0
